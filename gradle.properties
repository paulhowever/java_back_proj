# Gradle picks JDK 17 automatically via the toolchain declared in build.gradle.
# Do not set org.gradle.java.home here — it would pin Gradle to a host-specific
# path and break the Docker build (the path does not exist inside the image).
