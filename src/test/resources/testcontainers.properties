# Use Testcontainers auto-discovery for the current environment.
#
# Skip the docker /info-based environment validation: Docker Engine 29.x
# returns HTTP 400 on /info with an empty body, which Testcontainers'
# DockerClientProviderStrategy interprets as a fatal failure even though
# the daemon is otherwise reachable and can pull/run containers.
checks.disable=true
